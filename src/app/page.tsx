"use client";

import useLog from "@/app/hook/useLog";

export default function Home() {
    const [step, setStep, isPause, setIsPause, simulation, setSimulation , fileError] = useLog();

    return <></>;
}
